base_dir = ".fozzy"

[package]
name = "megaserver"
version = "0.1.0"

[[target.bin]]
name = "megaserver"
path = "src/main.fzy"

[ffi]
panic_boundary = "error"

[unsafe]
contracts = "compiler"
enforce_dev = false
enforce_verify = true
enforce_release = true
deny_unsafe_in = []
allow_unsafe_in = []
